const authority = import.meta.env.VITE_AUTH0_AUTHORITY;
const authClientId = import.meta.env.VITE_AUTH0_CLIENT_ID;
const authClientSecret = import.meta.env.VITE_AUTH0_CLIENT_SECRET;

export { authority, authClientId, authClientSecret };
